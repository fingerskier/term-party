# term-party
Multi-terminal manager

* Electron app
* Add a terminal by selecting a directory
* Show a list of open terminals
* Selecting a terminal in the list "opens" it in a viewer (and closes previous viewer (but not the terminal process))
* Can explicitly kill a terminal process

## UI

Left Panel: list of open terminals and a button to open another
Right Panel: the active terminal viewer
